# Tacharchy Audio Systemd Tmpfiles Configuration
# Sets per-core EPP for Intel hybrid CPUs
# This file is processed by the install script only on Intel hybrid systems.
#
# Format: type path mode user group age argument
# w = write lines, f = create/truncate file
#
# These are template entries; the install script customizes the actual values
# based on detected CPU topology (which P-cores to assign).
